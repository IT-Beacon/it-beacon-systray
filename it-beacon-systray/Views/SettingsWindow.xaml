<Window x:Class="it_beacon_systray.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:it_beacon_systray.Views"
        xmlns:helpers="clr-namespace:it_beacon_systray.Helpers"
        mc:Ignorable="d"
        Title="Application Settings" 
        Height="600" Width="800"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="True"
        ResizeMode="NoResize"
        Style="{StaticResource PopupWindowStyle}"
        Background="{DynamicResource PopupBackgroundBrush}"
        Foreground="{DynamicResource PopupForegroundBrush}">

    <Window.Resources>
        <!-- String and Boolean Converters -->
        <helpers:StringToBooleanConverter x:Key="StringToBooleanConverter"/>
        <helpers:InvertBooleanConverter x:Key="InvertBooleanConverter"/>
        <helpers:StringToGlyphConverter x:Key="StringToGlyphConverter"/>

        <!-- Modern Scrollbar Styles -->
        <Style x:Key="ScrollBarThumbStyle" TargetType="{x:Type Thumb}">
            <Setter Property="Width" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Border CornerRadius="3"
                                Background="{DynamicResource PopupBorderBrush}"
                                Opacity="0.6"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type ScrollBar}">
            <Setter Property="Stylus.IsPressAndHoldEnabled" Value="false"/>
            <Setter Property="Stylus.IsFlicksEnabled" Value="false"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid x:Name="Bg" SnapsToDevicePixels="true">
                            <Track x:Name="PART_Track"
                                   IsDirectionReversed="true">
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ScrollBarThumbStyle}" />
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Width" Value="Auto"/>
                    <Setter Property="MinWidth" Value="0"/>
                    <Setter Property="Height" Value="8"/>
                    <Setter Property="MinHeight" Value="8"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ScrollBar}">
                                <Grid x:Name="Bg" SnapsToDevicePixels="true">
                                    <Track x:Name="PART_Track">
                                        <Track.Thumb>
                                            <Thumb Style="{StaticResource ScrollBarThumbStyle}" />
                                        </Track.Thumb>
                                    </Track>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernScrollViewerStyle" TargetType="{x:Type ScrollViewer}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollViewer}">
                        <Grid x:Name="Grid" Background="{TemplateBinding Background}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter"
                                                    Grid.Column="0"
                                                    CanContentScroll="{TemplateBinding CanContentScroll}"
                                                    CanHorizontallyScroll="False"
                                                    CanVerticallyScroll="False"
                                                    ContentTemplate="{TemplateBinding ContentTemplate}"
                                                    Content="{TemplateBinding Content}"
                                                    Margin="{TemplateBinding Padding}"/>
                            <ScrollBar x:Name="PART_VerticalScrollBar"
                                       AutomationProperties.AutomationId="VerticalScrollBar"
                                       Cursor="Arrow"
                                       Grid.Column="1"
                                       Maximum="{TemplateBinding ScrollableHeight}"
                                       Minimum="0"
                                       ViewportSize="{TemplateBinding ViewportHeight}"
                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                                       Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"
                                       Opacity="0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="PART_VerticalScrollBar" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="PART_VerticalScrollBar" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.5"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- End of Modern Scrollbar Styles -->

        <!-- Setting TextBox Style -->
        <Style x:Key="SettingTextBoxStyle" TargetType="TextBox">
             <Setter Property="Padding" Value="5,3"/>
             <Setter Property="Background" Value="{DynamicResource PopupBackgroundBrush}"/>
             <Setter Property="Foreground" Value="{DynamicResource PopupForegroundBrush}"/>
             <Setter Property="BorderThickness" Value="1"/>
             <Setter Property="BorderBrush" Value="{DynamicResource PopupBorderBrush}"/>
             <Setter Property="VerticalContentAlignment" Value="Top"/>
             <Setter Property="Validation.ErrorTemplate" Value="{x:Null}"/>
			<Style.Triggers>
				<Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                </Trigger>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                    <Setter Property="Background" Value="{DynamicResource PopupBackgroundBrush}"/>
                </Trigger>
                <Trigger Property="IsReadOnly" Value="True">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Opacity" Value="0.6"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Setting Templates -->
        <DataTemplate x:Key="SettingTextTemplate">
            <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                     IsReadOnly="{Binding IsReadOnly}"
                     Style="{StaticResource SettingTextBoxStyle}"/>
        </DataTemplate>

        <DataTemplate x:Key="SettingMultiLineTextTemplate">
            <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                     IsReadOnly="{Binding IsReadOnly}"
                     Style="{StaticResource SettingTextBoxStyle}"
                     MinHeight="80"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     KeyDown="MultiLineTextBox_KeyDown"/>
        </DataTemplate>

        <DataTemplate x:Key="SettingGlyphTemplate">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding Value, Converter={StaticResource StringToGlyphConverter}}" FontFamily="Segoe Fluent Icons" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox Grid.Column="1" Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                         IsReadOnly="{Binding IsReadOnly}"
                         Style="{StaticResource SettingTextBoxStyle}"/>
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="SettingCheckBoxTemplate">
            <CheckBox IsChecked="{Binding Value, Converter={StaticResource StringToBooleanConverter}, UpdateSourceTrigger=PropertyChanged}"
                      IsEnabled="{Binding IsReadOnly, Converter={StaticResource InvertBooleanConverter}}"
                      VerticalAlignment="Center"/>
        </DataTemplate>

        <!-- Reverted Template Selector - Removed ImageTemplate Property -->
        <helpers:SettingTemplateSelector x:Key="SettingTemplateSelector"
                                         TextTemplate="{StaticResource SettingTextTemplate}"
                                         CheckBoxTemplate="{StaticResource SettingCheckBoxTemplate}"
                                         MultiLineTextTemplate="{StaticResource SettingMultiLineTextTemplate}"
                                         GlyphTemplate="{StaticResource SettingGlyphTemplate}"/>

        <!-- Category Item Style (Must be defined before CategoryListStyle if referenced via StaticResource, but ok with Dynamic) -->
        <Style x:Key="CategoryItemStyle" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Margin" Value="5,2"/>
            <Setter Property="Foreground" Value="{DynamicResource PopupForegroundBrush}"/>
            <Setter Property="Opacity" Value="0.8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="border"
                                Background="Transparent"
                                BorderBrush="Transparent"
                                BorderThickness="2,0,0,0"
                                Padding="{TemplateBinding Padding}"
                                Margin="{TemplateBinding Margin}"
                                CornerRadius="4">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentHoverBrush}"/>
                                <Setter Property="Opacity" Value="1"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentHoverBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Opacity" Value="1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Category List Style -->
        <Style x:Key="CategoryListStyle" TargetType="ListBox">
            <Setter Property="Background" Value="{DynamicResource FooterBackgroundBrush}" />
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="ItemContainerStyle" Value="{DynamicResource CategoryItemStyle}"/>
        </Style>

        <!-- Action Button Style -->
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="15,5"/>
            <Setter Property="MinWidth" Value="30"/> <!-- Adjusted for Browse button -->
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                             <Trigger Property="IsMouseOver" Value="True">
                                 <Setter Property="Opacity" Value="0.9"/>
                             </Trigger>
                             <Trigger Property="IsEnabled" Value="False">
                                 <Setter Property="Opacity" Value="0.5"/>
                                 <Setter Property="Cursor" Value="Arrow"/>
                             </Trigger>
                        </ControlTemplate.Triggers> 
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CancelButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="15,5"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource PopupForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource PopupBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource FooterBackgroundBrush}"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="{DynamicResource PopupBorderBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource PopupForegroundBrush}"/>
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="180"/>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" 
                Background="{DynamicResource FooterBackgroundBrush}"
                BorderBrush="{DynamicResource PopupBorderBrush}"
                BorderThickness="0,0,1,0">
            <DockPanel Margin="5">
                <TextBlock DockPanel.Dock="Top" 
                           Text="Categories" 
                           FontWeight="SemiBold" 
                           Opacity="0.7"
                           Margin="10,10,10,5"/>
                <TextBlock DockPanel.Dock="Bottom"
                           x:Name="VersionLabel"
                           Opacity="0.6"
                           Margin="10,5"
                           FontSize="11"/>
                <ListBox x:Name="CategoryListBox"
                         Style="{StaticResource CategoryListStyle}"
                         SelectionChanged="CategoryListBox_SelectionChanged"
                         ItemsSource="{Binding Categories}"/>
            </DockPanel>
        </Border>

        <Border Grid.Column="1" 
                Width="1" 
                Background="{DynamicResource PopupBorderBrush}"
                Opacity="0.5"/>

        <Border Grid.Column="2" 
                Background="{DynamicResource PopupBackgroundBrush}"
                Padding="15">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" 
                           x:Name="SettingsHeader"
                           Text="Loaded Configuration" 
                           FontSize="18" FontWeight="SemiBold" 
                           Margin="0,0,0,15"/>

                <StackPanel DockPanel.Dock="Bottom" 
                            Orientation="Horizontal" 
                            HorizontalAlignment="Right"
                            Margin="0,15,0,0">
                    <Button x:Name="RevertAdminButton" 
                            Content="Relaunch as User" 
                            Style="{StaticResource ActionButtonStyle}"
                            Visibility="Collapsed"
                            Click="RevertAdminButton_Click"
                            ToolTip="Relaunch application as a standard user."/>
                    <Button x:Name="OkButton" 
                            Content="OK" 
                            Style="{StaticResource ActionButtonStyle}"
                            Click="OkButton_Click"/>
                    <Button x:Name="CancelButton" 
                            Content="Cancel" 
                            Style="{StaticResource CancelButtonStyle}"
                            Click="CancelButton_Click"/>
                    <Button x:Name="ApplyButton" 
                            Content="Apply" 
                            Style="{StaticResource ActionButtonStyle}"
                            Click="ApplyButton_Click"/>
                </StackPanel>

                <TextBlock DockPanel.Dock="Bottom"
                           x:Name="ConfigPathLabel"
                           Opacity="0.6"
                           Margin="0,0,0,10"
                           FontSize="11"
                           TextWrapping="Wrap"/>

                <ScrollViewer VerticalScrollBarVisibility="Auto" 
                              HorizontalScrollBarVisibility="Disabled"
                              Style="{StaticResource ModernScrollViewerStyle}">
                    <ItemsControl ItemsSource="{Binding FilteredSettings}"
                                  Foreground="{DynamicResource PopupForegroundBrush}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,0,0,15">
                                    <!-- Default layout: Label on top, control below -->
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Key}" FontWeight="SemiBold" Opacity="0.8" Margin="0,0,0,5" TextWrapping="Wrap"/>
                                        <ContentPresenter Content="{Binding}" ContentTemplateSelector="{StaticResource SettingTemplateSelector}"/>
                                        <StackPanel.Style>
                                            <Style TargetType="StackPanel">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsType}" Value="boolean">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>
                                    </StackPanel>

                                    <!-- Special layout for checkboxes: Checkbox on left, label on right -->
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <ContentPresenter Content="{Binding}" ContentTemplateSelector="{StaticResource SettingTemplateSelector}" VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding Key}" FontWeight="SemiBold" Opacity="0.8" Margin="8,0,0,0" VerticalAlignment="Center" TextWrapping="Wrap"/>
                                        <StackPanel.Style>
                                            <Style TargetType="StackPanel">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsType}" Value="boolean">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

            </DockPanel>
        </Border>
    </Grid>
</Window>